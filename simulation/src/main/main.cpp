#include <iostream>

#include <card.hpp>
#include <deck.hpp>

int main(){
    /*
    Deck *deck = new Deck();
    for (int i=0; i<5; i++){
        deck->add_card_on_top(Card('J', 'S'));

    }
    for (Card card: deck->get_card_list()){
        printf("%c %c\n", card.get_rank(), card.get_suit());
    }*/
    int *r;
    int a = *r;
    a = 2;
    std::cout << "RUN\n";
    std::cout << *r << " " << a;

}